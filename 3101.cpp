#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;
#define ll long long

int main(){
  ios_base::sync_with_stdio(0);

  int n,k;
  cin >> n >> k;

  vector<char> v(k);
  for(int i=0 ;i<k;i++) cin >> v[i];
  ll ans = 1;
  for(char i : v){
    
  }

}
