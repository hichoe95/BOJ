#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
#define ll long long

int main(){

  int n;
  cin >> n;
  vector<pair<ll,ll> > s(n);

  for(int i=0 ;i<n;i++)
    cin >> s[i].first >> s[i].second;

  

  return 0;
}
